plugins {
    id 'base'
}

repositories {
    jcenter()
}

task pythonBuild(type: Exec) {
    description = 'Builds the Python project.'
    commandLine 'python', 'setup.py', 'build'
}

build.dependsOn pythonBuild
